# DH-ChatGPT-Tetris

A dependency-free Node.js Tetris game built for Deploy Hatch.

## Run locally

```bash
npm start
```

Open `http://localhost:3000`. The server exposes `GET /health` for platform readiness checks.

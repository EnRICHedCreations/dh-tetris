# Neon Stack

A zero-dependency Node.js 22 Tetris game built for Deploy Hatch. The server binds to `0.0.0.0` and honors the platform `PORT` environment variable.

```bash
npm test
npm start
```

Open `http://localhost:3000`. Health checks are available at `/health`.
